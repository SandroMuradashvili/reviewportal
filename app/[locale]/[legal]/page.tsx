import { notFound } from "next/navigation";import { SiteHeader } from "@/components/site-header";import { SiteFooter } from "@/components/site-footer";import { getLocale } from "@/lib/i18n";import { legalContent } from "@/lib/legal";
export default async function Legal({params}:{params:Promise<{locale:string;legal:string}>}){const {locale:raw,legal}=await params;const locale=getLocale(raw),content=legalContent[legal as keyof typeof legalContent];if(!content)notFound();const doc=content[locale];return <><SiteHeader locale={locale}/><main className="legal"><div className="eyebrow">Effective · 16 August 2026</div><h1>{doc.title}</h1><p>{doc.intro}</p>{doc.sections.map(([h,p])=><section key={h}><h2>{h}</h2><p>{p}</p></section>)}<p><strong>Launch note:</strong> Insert the operator’s registered legal name, identification number and registered address before accepting payments. Georgian-qualified counsel should approve the controlling Georgian text.</p></main><SiteFooter locale={locale}/></>}
