import { SiteHeader } from "@/components/site-header";import { SiteFooter } from "@/components/site-footer";import { getLocale } from "@/lib/i18n";import { whatsapp } from "@/lib/data";
export default async function Contact({params}:{params:Promise<{locale:string}>}){const locale=getLocale((await params).locale);return <><SiteHeader locale={locale}/><main className="legal"><div className="eyebrow">Contact</div><h1>Let’s talk</h1><p>Questions about setup, NFC products, activation, privacy, or your account? We reply personally.</p><a className="button" href={whatsapp("Hello ReviewPortal, I'd like to learn more.")} target="_blank" rel="noreferrer">WhatsApp · +995 577 66 55 25</a><p>Email: support@reviewportal.ge</p></main><SiteFooter locale={locale}/></>}
