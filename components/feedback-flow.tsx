"use client";
import { ExternalLink,Star } from "lucide-react";
import Image from "next/image";
import Link from "next/link";
import { useEffect,useState } from "react";
import { copy,Locale } from "@/lib/i18n";
import { useMutation,useQuery } from "convex/react";
import { api } from "@/convex/_generated/api";

const demoGoogleReviewUrl="https://g.page/r/CbYRI-aSFb10EBM/review";
const stateCopy={ka:{loading:"იტვირთება…",missing:"ეს შეფასების ბმული ვერ მოიძებნა.",unavailable:"ეს შეფასების ბმული ამჟამად მიუწვდომელია.",trial:"ამ ბიზნესის საცდელი ლიმიტი ამოიწურა.",google:"დატოვე Google შეფასება",retry:"გთხოვთ, სცადოთ თავიდან.",privatePrompt:"ვწუხვართ, რომ გამოცდილება იდეალური არ იყო. მოგვწერეთ, რა მოხდა, რათა პრობლემა მოვაგვაროთ.",comment:"რა შეგვიძლია გავაუმჯობესოთ?"},en:{loading:"Loading…",missing:"This review link could not be found.",unavailable:"This review link is not available right now.",trial:"This business has reached its trial response limit.",google:"Leave a Google review",retry:"Please try again.",privatePrompt:"We’re sorry your experience wasn’t ideal. Tell us what happened so the business can make it right.",comment:"What could we improve?"},ru:{loading:"Загрузка…",missing:"Ссылка для отзывов не найдена.",unavailable:"Ссылка для отзывов сейчас недоступна.",trial:"Компания достигла лимита пробных ответов.",google:"Оставить отзыв в Google",retry:"Попробуйте ещё раз.",privatePrompt:"Нам жаль, что ваш опыт оказался неидеальным. Расскажите, что произошло, чтобы компания могла решить проблему.",comment:"Что мы можем улучшить?"}};

export function FeedbackFlow({locale,business:demoBusiness,slug}:{locale:Locale;business:string;slug:string}){
  const demo=slug==="demo",portal=useQuery(api.publicPortal.bySlug,demo?"skip":{slug}),submitFeedback=useMutation(api.feedback.submit),recordVisit=useMutation(api.publicPortal.visit),recordStar=useMutation(api.publicPortal.starSelected),recordRedirect=useMutation(api.publicPortal.redirected);
  const t=copy[locale],s=stateCopy[locale],[rating,setRating]=useState(0),[comment,setComment]=useState(""),[sent,setSent]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState(""),[token,setToken]=useState("");
  useEffect(()=>{if(demo)return;void getVisitToken(slug).then(value=>{setToken(value);void recordVisit({slug,visitTokenHash:value})})},[demo,recordVisit,slug]);
  if(!demo&&portal===undefined)return <StateCard text={s.loading}/>;
  if(!demo&&portal?.state!=="ready")return <StateCard text={portal?.state==="missing"?s.missing:portal?.state==="trial-ended"?s.trial:s.unavailable}/>;
  const readyPortal=!demo&&portal?.state==="ready"?portal:null,business=demo?demoBusiness:readyPortal?.name??"ReviewPortal",prompt=demo?t.feedback:readyPortal?.prompt?.[locale]??t.feedback,destination=demo?undefined:readyPortal?.destinationUrl,threshold=demo?4:readyPortal?.redirectThreshold??4;
  async function select(value:number){setRating(value);if(demo&&value>=4){window.location.assign(demoGoogleReviewUrl);return}if(!demo&&token)await recordStar({slug,visitTokenHash:token,rating:value})}
  async function submit(){if(!rating)return;setBusy(true);setError("");try{if(demo)await new Promise(resolve=>setTimeout(resolve,250));else await submitFeedback({slug,visitTokenHash:token||await getVisitToken(slug),rating,comment:comment||undefined});setSent(true)}catch(reason){setError(reason instanceof Error?reason.message:s.retry)}finally{setBusy(false)}}
  async function goToGoogle(){if(!destination)return;if(!demo&&token)await recordRedirect({slug,visitTokenHash:token});window.location.assign(destination)}
  return <main className={`feedback-page ${demo?"demo-feedback-page":""}`}><section className={`feedback-card ${demo?"demo-feedback-card":""}`} aria-live="polite">{demo?<nav className="demo-locales" aria-label="Language">{(["ka","en","ru"] as const).map(item=><Link key={item} className={item===locale?"active":""} href={`/${item}/r/demo`}>{item.toUpperCase()}</Link>)}</nav>:null}<div className="portal-brand-mark">{demo?<Image src="/brand/secondary.svg" alt="ReviewPortal" width={46} height={46}/>:business.slice(0,1)}</div><h1 className="portal-title">{business}</h1>{sent?<><div className="success-mark">✓</div><h2 className="portal-thanks">{t.thanks}</h2><p className="disclosure">{t.private}</p>{destination&&rating>=threshold?<button className="button google-review" onClick={()=>void goToGoogle()}>{s.google}<ExternalLink size={17}/></button>:null}</>:<><p className="portal-prompt">{prompt}</p><div className="stars" role="radiogroup" aria-label={prompt}>{[1,2,3,4,5].map(n=><button key={n} className={`star ${n<=rating?"active":""}`} onClick={()=>void select(n)} role="radio" aria-checked={rating===n} aria-label={`${n} out of 5`}><Star size={42} fill={n<=rating?"currentColor":"none"}/></button>)}</div>{rating>0&&(!demo||rating<4)?<><p className="private-followup">{demo?s.privatePrompt:null}</p><label htmlFor="comment" className="comment-label">{demo?s.comment:t.comment}</label><textarea id="comment" required={demo} maxLength={1000} value={comment} onChange={e=>setComment(e.target.value)}/><button className="button full-button" disabled={busy||(!demo&&!token)||(demo&&!comment.trim())} onClick={submit}>{busy?"…":t.send}</button></>:null}{error?<p className="auth-error" role="alert">{error}</p>:null}<p className="disclosure">{t.private}<br/>Powered by ReviewPortal · <a href={`/${locale}/privacy`}>Privacy</a></p></>}</section></main>
}

function StateCard({text}:{text:string}){return <main className="feedback-page"><section className="feedback-card"><ImageMark/><h1 className="portal-thanks">{text}</h1><Link className="button secondary" href="/ka">ReviewPortal</Link></section></main>}
function ImageMark(){return <div className="phone-logo" style={{display:"grid",placeItems:"center",fontWeight:800,color:"var(--green)"}}>R</div>}
async function getVisitToken(slug:string){const key=`rp_visit_${slug}`,existing=localStorage.getItem(key);if(existing)return existing;const raw=crypto.randomUUID(),bytes=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(raw)),token=Array.from(new Uint8Array(bytes),byte=>byte.toString(16).padStart(2,"0")).join("");localStorage.setItem(key,token);return token}
