# Created by `npx convex dev`
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=

# Create an OAuth 2.0 Web application in Google Cloud.
# Configure the callback printed by `npx convex env get SITE_URL` + `/api/auth/callback/google`.
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Production public URL, e.g. https://reviewportal.ge
NEXT_PUBLIC_SITE_URL=http://localhost:3000
